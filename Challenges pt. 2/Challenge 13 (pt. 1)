<apex:page controller="customControl">
	<apex:form>
    	<apex:pageBlock>
        	<apex:pageBlockTable value="{!conList}" var="con">
                <apex:column value="{!con.name}" />
                <apex:column value="{!con.phone}" />
                <apex:column value="{!con.email}" />
            </apex:pageBlockTable>
            <apex:outputText value="{!hateMessage}" />
        </apex:pageBlock>
        <apex:commandButton action="{!changeToCA}" value="Press to view Californians" />
    </apex:form>
</apex:page>
